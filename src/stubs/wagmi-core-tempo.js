// Stub for @wagmi/core/tempo — this export was removed in @wagmi/core v2.22+
// but @wagmi/connectors (bundled inside @reown/appkit-adapter-wagmi) still
// references it. tempoWallet is not used in this project so a no-op stub is safe.
export const tempoWallet = undefined
